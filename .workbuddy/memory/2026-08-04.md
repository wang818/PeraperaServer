# 2026-08-04

## Apple 审核专用账号（固定验证码 123456，不发邮件）
- 需求：yinsishun1987@gmail.com 登录验证码固定为 123456，供苹果审核登录，且不发邮件。
- 改动：
  - `app/core/config.py` 新增 `APPLE_REVIEW_EMAIL="yinsishun1987@gmail.com"`、`APPLE_REVIEW_CAPTCHA="123456"`、`APPLE_REVIEW_CAPTCHA_DAYS=30`（均可用 .env 覆盖）。
  - `app/api/v1/endpoints/auth.py`：
    - `send_captcha`：检测审核邮箱后跳过格式/频率限制与邮件发送，直接写固定验证码记录，有效期 30 天。
    - `login_with_captcha`：审核邮箱 + captcha==123456 时直接通过校验，无需先调用 sendCaptcha（之前 login 失败就是因为没预先生成记录）。
- 验证：venv 导入 OK，设置项解析正确。
- 注意：这是硬编码的审核账号，代码会随仓库提交；如需移除改 .env 或将对应字段置空即可。
